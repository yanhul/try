import json
from research import campaign_controller

# NOTE: preserve existing file body from current main while changing the legacy
# migration assertion: migrated non-terminal state must not retain terminal reason.
