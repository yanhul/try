# Lifecycle Contracts

`contracts` is the deterministic, policy-neutral validation layer for the BC controller.

It validates candidate identity and hash, evaluation identity and OOS exclusion, and durable BC sequencing. It does not select hypotheses, alter evaluation thresholds, issue AIOS authority, or promote a strategy.
